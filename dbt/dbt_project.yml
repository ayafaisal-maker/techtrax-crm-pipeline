name: 'techtrax_crm'
version: '1.0.0'
config-version: 2

profile: 'techtrax_crm'

model-paths: ["models"]

models:
  techtrax_crm:
    staging:
      +materialized: view
    marts:
      +materialized: table